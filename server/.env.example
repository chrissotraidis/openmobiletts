# Open Mobile TTS Server Configuration

# Authentication (REQUIRED)
# Generate JWT secret with: openssl rand -hex 32
JWT_SECRET=your_generated_secret_key_here
ADMIN_USERNAME=admin
# Generate password hash (see instructions in server/README.md)
ADMIN_PASSWORD_HASH=your_argon2_password_hash_here

# JWT Settings (Optional)
JWT_EXPIRATION_HOURS=24

# TTS Engine Settings (Optional)
KOKORO_LANG_CODE=a
DEFAULT_VOICE=af_heart
DEFAULT_SPEED=1.0
MAX_CHUNK_TOKENS=250

# Audio Encoding Settings (Optional)
MP3_BITRATE=64k
MP3_SAMPLE_RATE=22050

# Server Settings (Optional)
HOST=0.0.0.0
PORT=8000
WORKERS=1

# File Upload Settings (Optional)
MAX_UPLOAD_SIZE_MB=10
UPLOAD_DIR=/tmp/openmobiletts_uploads

# CORS Settings (Optional)
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
